[profile.default]
src = "contracts/minbasefee"
out = "out"
libs = ["lib"]
test = "contracts/minbasefee/test"
cache_path = "cache_forge"

solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200
via_ir = false
evm_version = "paris"

[profile.default.fuzz]
runs = 256

[profile.ci]
fuzz = { runs = 5000 }
invariant = { runs = 1000 }
